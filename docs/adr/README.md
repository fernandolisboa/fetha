# Architecture Decision Records

One file per decision: `NNNN-title.md` with sections Context, Decision, Consequences, Alternatives.
Accepted ADRs are never edited; a later ADR supersedes them and says so.

Planned for Phase 1 and 2: engine boundary, strategy DSL, data-provider strategy, backtest hygiene,
AI decision contract, numeric representation, engine public interface (frozen).
